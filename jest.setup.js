// Global test setup for jest-expo. Kept intentionally small — Phase 0's test
// suite targets pure business logic (auth error classification, uuid
// generation, permission-status normalization), which needs no native mocks
// beyond what jest-expo's preset already provides.
